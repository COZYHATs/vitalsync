const id=new URLSearchParams(location.search).get('id');document.addEventListener('DOMContentLoaded',async()=>{if(!id)return location='documents.html';try{let d=await api('/documents/'+id);name.textContent=d.original_filename;detail.innerHTML=`<div class="detail-card"><strong>Document type</strong><p>${esc(d.document_type)}</p></div><div class="detail-card"><strong>Upload date</strong><p>${new Date(d.created_at).toLocaleString()}</p></div><div class="detail-card"><strong>File size</strong><p>${d.file_size} bytes</p></div><div class="detail-card"><strong>Processing status</strong><p><span class="status-pill ${statusClass(d.processing_status)}">${esc(statusLabel(d.processing_status))}</span></p></div><div class="detail-card" style="grid-column: 1 / -1;"><strong>Integrity hash</strong><code>${esc(d.file_hash)}</code></div>`;view.href=API+'/documents/'+id+'/file';request.onclick=async()=>{let reason=prompt('Reason for deletion request (optional):')??null;if(reason===null)return;try{await api('/documents/'+id+'/deletion-request',{method:'POST',body:JSON.stringify({reason})});setMessage('Deletion request submitted for administrator review.','success');request.disabled=true}catch(e){setMessage(e.message,'error')}}}catch(e){setMessage(e.message,'error')}})
